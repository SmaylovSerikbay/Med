# PhoneInput Component - Документация

## Обзор

Красивый компонент для ввода номера телефона с автоматическим форматированием, валидацией и визуальной обратной связью.

## Возможности

✅ **Автоматическое форматирование** - +7 (700) 123-45-67  
✅ **Валидация в реальном времени** - зеленая/красная индикация  
✅ **Маска ввода** - только цифры  
✅ **Визуальная обратная связь** - иконки и цвета  
✅ **Подсказки** - формат и ошибки  
✅ **Доступность** - полная поддержка клавиатуры  

## Использование

### Базовый пример

```tsx
import { PhoneInput } from '@/components/ui/PhoneInput'

function MyForm() {
  const [phone, setPhone] = useState('')

  return (
    <PhoneInput
      value={phone}
      onChange={setPhone}
      label="Номер телефона"
      required
    />
  )
}
```

### С обработкой ошибок

```tsx
import { PhoneInput } from '@/components/ui/PhoneInput'

function MyForm() {
  const [phone, setPhone] = useState('')
  const [error, setError] = useState('')

  const handleSubmit = () => {
    if (!phone || phone.length !== 11) {
      setError('Введите корректный номер')
      return
    }
    // Отправка формы
  }

  return (
    <PhoneInput
      value={phone}
      onChange={setPhone}
      error={error}
      required
    />
  )
}
```

## Props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `value` | `string` | - | Значение номера (только цифры, например "77001234567") |
| `onChange` | `(value: string) => void` | - | Callback при изменении значения |
| `label` | `string` | "Номер телефона" | Текст метки |
| `error` | `string` | - | Текст ошибки |
| `disabled` | `boolean` | `false` | Отключить поле |
| `required` | `boolean` | `false` | Обязательное поле |
| `placeholder` | `string` | "+7 (___) ___-__-__" | Placeholder |

## Формат данных

### Входное значение (value)
Компонент ожидает **чистый номер** (только цифры):
```
"77001234567"  ✅ Правильно
"+77001234567" ❌ Неправильно
"7 700 123 45 67" ❌ Неправильно
```

### Выходное значение (onChange)
Компонент возвращает **нормализованный номер**:
```typescript
onChange("77001234567") // Всегда начинается с 7, только цифры
```

### Отображение
Пользователь видит **форматированный номер**:
```
+7 (700) 123-45-67
```

## Валидация

### Автоматическая валидация
Компонент автоматически проверяет:
- Длина номера (11 цифр)
- Начинается с 7
- Только цифры

### Визуальная индикация

| Состояние | Цвет | Иконка |
|-----------|------|--------|
| Пустое поле | Серый | 📱 |
| Валидный номер | Зеленый | ✅ |
| Невалидный номер | Красный | ❌ |
| Ошибка | Красный | ⚠️ |

### Сообщения

```
✅ "Номер телефона корректен"
❌ "Неверный формат номера телефона"
ℹ️ "Формат: +7 (700) 123-45-67"
```

## Поведение

### При вводе
1. Пользователь вводит цифры
2. Компонент автоматически форматирует
3. Показывает валидацию в реальном времени
4. Ограничивает до 11 цифр

### При фокусе
- Если поле пустое, добавляется "+7 "
- Курсор устанавливается после префикса

### При потере фокуса
- Финальное форматирование
- Показ валидации
- Показ ошибок

### Клавиатура
- **Цифры** - ввод
- **Backspace/Delete** - удаление
- **Стрелки** - навигация
- **Tab** - переход к следующему полю
- **Другие клавиши** - блокируются

## Утилиты (phoneUtils.ts)

### formatPhoneNumber()
Форматирует номер для отображения:
```typescript
formatPhoneNumber("77001234567")
// Результат: "+7 (700) 123-45-67"
```

### normalizePhoneNumber()
Нормализует номер (убирает форматирование):
```typescript
normalizePhoneNumber("+7 (700) 123-45-67")
// Результат: "77001234567"

normalizePhoneNumber("8 700 123 45 67")
// Результат: "77001234567" (8 заменяется на 7)
```

### isValidPhoneNumber()
Проверяет валидность:
```typescript
isValidPhoneNumber("77001234567") // true
isValidPhoneNumber("7700123456")  // false (10 цифр)
isValidPhoneNumber("87001234567") // false (начинается с 8)
```

### maskPhoneNumber()
Маскирует номер для безопасности:
```typescript
maskPhoneNumber("77001234567")
// Результат: "+7 (700) ***-**-67"
```

## Примеры использования

### В форме входа
```tsx
<PhoneInput
  value={phoneNumber}
  onChange={setPhoneNumber}
  label="Номер телефона"
  required
  error={error}
  disabled={isLoading}
/>
```

### В профиле (только чтение)
```tsx
<div>
  <label>Номер телефона</label>
  <p>{formatPhoneNumber(user.phone_number)}</p>
</div>
```

### В списке (замаскированный)
```tsx
<div>
  <span>{maskPhoneNumber(user.phone_number)}</span>
</div>
```

## Стилизация

### Цветовая схема

```css
/* Нормальное состояние */
border: gray-300
focus: primary-500

/* Валидный номер */
border: green-300
focus: green-500
icon: green-500

/* Невалидный номер */
border: red-300
focus: red-500
icon: red-500

/* Отключено */
background: gray-50
cursor: not-allowed
```

### Кастомизация

Компонент использует Tailwind CSS классы. Для изменения стилей отредактируйте `PhoneInput.tsx`:

```tsx
className={`
  w-full pl-10 pr-10 py-2.5 
  border rounded-lg 
  // Ваши кастомные классы
`}
```

## Доступность (a11y)

✅ **Метки** - правильные label для screen readers  
✅ **ARIA** - aria-invalid при ошибках  
✅ **Клавиатура** - полная навигация  
✅ **Фокус** - видимые индикаторы  
✅ **Ошибки** - связаны с полем через aria-describedby  

## Производительность

- **Дебаунс** - нет (мгновенная обратная связь)
- **Мемоизация** - не требуется (легкий компонент)
- **Ре-рендеры** - минимальные (controlled component)

## Совместимость

- ✅ React 18+
- ✅ Next.js 14+
- ✅ TypeScript
- ✅ Все современные браузеры
- ✅ Мобильные устройства

## Тестирование

### Unit тесты
```typescript
describe('PhoneInput', () => {
  it('форматирует номер при вводе', () => {
    // Тест форматирования
  })
  
  it('валидирует номер', () => {
    // Тест валидации
  })
  
  it('ограничивает длину', () => {
    // Тест ограничения
  })
})
```

### E2E тесты
```typescript
test('пользователь может ввести номер телефона', async () => {
  await page.fill('[data-testid="phone-input"]', '77001234567')
  await expect(page.locator('[data-testid="phone-input"]')).toHaveValue('+7 (700) 123-45-67')
})
```

## Известные ограничения

1. **Только казахстанские номера** - начинаются с +7
2. **Фиксированная длина** - 11 цифр
3. **Нет международных номеров** - только +7

## Расширение

### Добавление других стран

```typescript
// Добавьте в PhoneInput.tsx
const COUNTRY_CODES = {
  KZ: { code: '7', length: 11, format: '+7 (XXX) XXX-XX-XX' },
  RU: { code: '7', length: 11, format: '+7 (XXX) XXX-XX-XX' },
  // ...
}
```

### Добавление маски для других форматов

```typescript
const formatPhoneNumber = (phone: string, country: string): string => {
  const config = COUNTRY_CODES[country]
  // Форматирование по конфигу
}
```

## FAQ

**Q: Почему компонент возвращает "77001234567" а не "+7 (700) 123-45-67"?**  
A: Для упрощения работы с API. Форматированный номер только для отображения.

**Q: Можно ли использовать для других стран?**  
A: Да, но нужно расширить логику форматирования и валидации.

**Q: Как отключить валидацию?**  
A: Валидация встроена, но можно игнорировать визуальные индикаторы.

**Q: Поддерживается ли копирование/вставка?**  
A: Да, компонент автоматически очищает и форматирует вставленный текст.

## Changelog

### v1.0.0 (26 ноября 2025)
- ✅ Первая версия
- ✅ Автоматическое форматирование
- ✅ Валидация в реальном времени
- ✅ Визуальная обратная связь
- ✅ Утилиты для работы с номерами

## Поддержка

Вопросы? Проблемы? Предложения?
- Проверьте примеры выше
- Изучите код компонента
- Создайте issue

---

**Версия:** 1.0.0  
**Автор:** ProfMed.kz Team  
**Лицензия:** Internal
